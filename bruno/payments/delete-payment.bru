meta {
  name: Delete Payment
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/payments/{{paymentId}}
  body: none
  auth: inherit
}

docs {
  DELETE /api/v1/payments/{id} — Delete payment and recalculate account balance. Administrator and Supervisor. Returns 204.
}
