meta {
  name: Update Guarantee Payment
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/v1/rental-guarantee/payments/{{guaranteePaymentId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "paymentDate": "2026-06-09",
    "note": "Updated reference"
  }
}

docs {
  PUT /api/v1/rental-guarantee/payments/{id} — Update paymentDate and note only. Immutable: amount, receiptNumber, paidBy*.
}
