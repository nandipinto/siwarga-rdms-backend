meta {
  name: Get Payment by ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/payments/{{paymentId}}
  body: none
  auth: inherit
}

docs {
  GET /api/v1/payments/{id} — Get payment with allocation breakdown. Administrator and Supervisor.
}
