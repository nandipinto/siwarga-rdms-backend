meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/users/{{userId}}
  body: none
  auth: inherit
}

docs {
  DELETE /api/v1/users/{id} — Delete user. Administrator only. Returns 204.
}
