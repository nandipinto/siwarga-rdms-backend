meta {
  name: Delete RW
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/rws/{{rwId}}
  body: none
  auth: inherit
}

docs {
  DELETE /api/v1/rws/{id} — Delete RW (only if no RTs linked). Administrator only. Returns 204.
}
